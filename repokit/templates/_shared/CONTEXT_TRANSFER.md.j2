# CONTEXT_TRANSFER

## Goal
- Track context imports from other repositories.

## Usage
- List available repositories: `repokit context inventory --scope <dir>`
- Copy context from one project to another:
  `repokit context transfer --from <src_repo> --to <dst_repo> --mode copy`
- Move instead of copy:
  `repokit context transfer --from <src_repo> --to <dst_repo> --mode move`

## Imported contexts
- (none yet)
